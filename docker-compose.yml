version: "3.9"

services:
  backend:
    build: ./backend
    container_name: issuepilot-backend
    ports:
      - "8000:8000"

  frontend:
    build: ./frontend/bug-triage
    container_name: issuepilot-frontend
    ports:
      - "5173:80"
    depends_on:
      - backend
